const initiaState = {
    
}


export default function app(state = initiaState, action) {

    switch (action.type) {

        case "EXAMPLE":{
            return state
        }
        
        default: {
            return state
        }
        
    }

}